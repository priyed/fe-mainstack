import { Container } from "@mui/material";
import Balance from "./balance";

const Dashboard = () => {
  return (
    <Container sx={{ mt: 7 }}>
      <Balance />
    </Container>
  );
};

export default Dashboard;
